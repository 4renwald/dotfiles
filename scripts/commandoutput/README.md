# Command Output Scripts

Place helper scripts for the `Command Output` Plasma widget here.

The installer syncs this directory to:

- `~/.local/share/plasma-commandoutput/scripts`

Point the widget to scripts in that location when configuring panel items.
